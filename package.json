{
  "name": "fullstack-react",
  "version": "0.1.0",
  "description": "Fullstack React - The Complete Guide to ReactJS and Friends",
  "main": "index.js",
  "scripts": {
    "release:commit": "git add CHANGELOG.md && git commit -m 'docs: changelog update'",
    "release:major": "changelog -M && npm run release:commit && npm version major && npm run release:push",
    "release:minor": "changelog -m && npm run release:commit && npm version minor && npm run release:push",
    "release:patch": "changelog -p && npm run release:commit && npm version patch && npm run release:push",
    "release:push": "git push origin && git push origin --tags",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@study-pm.gitlab.com:fullstack-react-pm/fullstack-react.git"
  },
  "keywords": [
    "study",
    "tutorial",
    "react"
  ],
  "author": "viktor",
  "license": "ISC",
  "engines": {
    "git": ">=2.11",
    "node": ">=16.17.0"
  },
  "os": [
    "linux"
  ],
  "private": true,
  "devDependencies": {
    "generate-changelog": "^1.8.0"
  }
}
